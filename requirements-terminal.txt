# Agent task (Terminal-Bench 2.0 / SWE-bench Verified). Requires Python 3.12.
# Install together with the base requirements in a Python 3.12 env:
#   pip install -r requirements.txt -r requirements-terminal.txt
harbor==0.3.0
litellm==1.82.6
tenacity==9.1.4
python-dotenv==1.2.2
terminal-bench @ git+https://github.com/harbor-framework/terminal-bench.git@1a6ffa9674b571da0ed040c470cb40c4d85f9b9b
